#pragma once

#include <Player.h>
#include <Ball.h>

/*
class Pong {
public:
    Pong() = default;

    void init();
    void run();

private:
    // Left
    Player player1;
    // Right
    Player player2;

    // The ball will be synced across both clients
    Ball ball;

    sf::RenderWindow window;
};
*/

class Client
{

};
